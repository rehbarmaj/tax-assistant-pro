import type { ReactNode } from 'react';

// This layout is obsolete and has been replaced by the one at /src/app/[locale]/(app)/layout.tsx
export default function ObsoleteAppLayout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
