
export default function RootLocalePage() {
  // This page is intentionally left blank.
  // The root redirect is handled by the middleware and `/app/page.tsx`.
  // It exists to provide a valid route for the root of a locale, e.g., /en,
  // which then gets redirected by other mechanisms.
  // In a real app, this might be a landing page for the specific locale.
  return null;
}
